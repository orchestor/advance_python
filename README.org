
1. Why do we use data model?
A: It provides unified method interfaces for difference classes. For example, we define two special methods: ~__len__~ and ~__getitme__~, which corresponds to ~len(obj)~ and ~obj[]~ API calls.     
